name: CI

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read
  pull-requests: write

jobs:
  ci:
    uses: promptping-ai/.github/.github/workflows/graphite-swift-ci.yml@main
    secrets: inherit

  rules:
    uses: promptping-ai/.github/.github/workflows/swift-rules-check.yml@main
    secrets: inherit

  workflow-lint:
    uses: promptping-ai/.github/.github/workflows/workflow-lint.yml@main

  review:
    uses: promptping-ai/.github/.github/workflows/swift-review.yml@main
    secrets: inherit
